# HealthConnect — Data Quality Report

Generated by `src.data.validate_data.validate_dataset` against
`data/raw/HealthConnect_Appointment_Data.csv` (Week 5).

```
HealthConnect Data Validation Report
========================================
Rows: 5000 | Columns: 18
Duplicate appointment_id count: 0
Fully duplicate rows: 0
Invalid appointment_outcome values: 0
Invalid reminder_sent values: 0
Invalid appointment_time values: 0
Unparseable dates: 0
previous_no_shows > previous_appointments: 0

Missing value counts:
  - reminder_channel: 1366
  - distance_to_clinic_km: 90
  - waiting_time_minutes: 60

Warnings:
  - booking_date/appointment_date parse successfully but are stored as
    M/D/YYYY text, not the ISO format stated in the data dictionary.
    Downstream code should not assume ISO format.

Overall result: PASSED
```

## Interpretation

- **No structural problems**: zero duplicate rows, zero duplicate
  `appointment_id` values, zero invalid categorical values, zero
  historical inconsistencies (`previous_no_shows > previous_appointments`).
- **Missing values are all expected and explainable**, per the Data
  Science handoff:
  - `reminder_channel` (27.32% missing) — verified structural, every null
    corresponds exactly to `reminder_sent == "No"`. Handled via an
    explicit `"None"` category, not imputation.
  - `distance_to_clinic_km` (1.80% missing) — handled via median
    imputation, fit on the training fold only.
  - `waiting_time_minutes` (1.20% missing) — irrelevant, since this column
    is dropped entirely due to confirmed data leakage (populated for
    98.5% of No-Show/Cancelled rows, which shouldn't have a real waiting
    time).
- **Date format mismatch**: `booking_date`/`appointment_date` are stored
  as `M/D/YYYY` text, not the ISO format the data dictionary describes.
  Parsed explicitly with `format="%m/%d/%Y"` rather than relying on
  pandas' format inference, to avoid silent misparsing.

## Target distribution

After excluding `Cancelled` (263 rows, 5.3%):

| Class | Count | % |
|---|---|---|
| No-Show | 2,423 | 51.2% |
| Attended | 2,314 | 48.8% |

Close to balanced — no resampling or class-weighting applied for the
baseline model.
